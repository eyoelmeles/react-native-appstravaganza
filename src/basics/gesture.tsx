import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GestureMovement = () => {
  return (
    <View>
      <Text>GestureMovement</Text>
    </View>
  );
};

export default GestureMovement;

const styles = StyleSheet.create({});
